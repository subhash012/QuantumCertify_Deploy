# QuantumCertify Environment Configuration Template
# Copy this file to .env and update the values according to your environment

# ===========================================
# Google Gemini AI Configuration
# ===========================================
# Your Google Gemini API key for AI-powered recommendations
GEMINI_API_KEY=your_gemini_api_key_here

# ===========================================
# Database Configuration (Azure SQL Server)
# ===========================================
# Azure SQL Server hostname
DB_SERVER=your-server.database.windows.net

# Database name
DB_NAME=YourDatabaseName

# Database username
DB_USERNAME=your_db_username

# Database password (keep this secure!)
DB_PASSWORD=your_secure_password

# Database port (default: 1433 for SQL Server)
DB_PORT=1433

# ODBC Driver (default: SQL+Server)
DB_DRIVER=SQL+Server

# ===========================================
# Application Configuration
# ===========================================
# Contact email for support
CONTACT_EMAIL=your-email@example.com

# Developer/Organization name
DEVELOPER_NAME=Your Name or Organization

# Application version
PROJECT_VERSION=2.0.0

# ===========================================
# Security Configuration
# ===========================================
# Secret key for application security (generate a strong random key)
SECRET_KEY=your-super-secret-key-change-this-in-production

# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://your-frontend-domain.com

# Debug mode (true/false)
DEBUG=false

# ===========================================
# Optional Configuration
# ===========================================
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Database connection pool settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_RECYCLE=3600

# API rate limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=100